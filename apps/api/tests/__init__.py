"""API test suite (Phase 0 Definition of Done).

A package rather than a bare directory so ``from tests.factories import ...`` works when
pytest runs from ``apps/api`` (which is on ``PYTHONPATH`` in CI and in the container).
"""
